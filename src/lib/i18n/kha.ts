// Khasi UI strings (GAP-16/US-801). Wiring is READY; the pack ships empty on
// purpose. Every key falls back to English (see translate() in ./index.ts), so
// selecting Khasi today renders English rather than blanks — translations can be
// filled in key-by-key without any code change. This is the local-language moat's
// first rung.
export const kha: Record<string, string> = {};
