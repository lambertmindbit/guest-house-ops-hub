# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# typescript incremental build cache
*.tsbuildinfo

# production
/build

# testing
/coverage

# env — never commit secrets
.env
.env*.local

# prisma
/prisma/*.db

# misc
.DS_Store
*.pem
npm-debug.log*
.vscode/

# local scratch / handoff drops — not part of the app
/_dropbox/
/docs/HANDOFF.html

# Editor lock files + local planning drops
~$*
ROOT_Lawei_Gap_Analysis*

# ROOT platform reference drop (separate repo; contains node_modules + a large zip)
/Tourism Agent/
/Tourism Agent.zip

# Reference agent-system drop for study (separate project; node_modules + .env inside)
/Archive/
